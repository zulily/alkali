aliases
=======

Simple /etc/aliases management, based on pillar data.  Requires the postfix
states run first, handled by top.sls ordering.  Customize aliases by
editing aliases.sls in the pillar tree.
